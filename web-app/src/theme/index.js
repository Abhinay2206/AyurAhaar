import { colors } from './colors.js';
import { typography } from './typography.js';
import { spacing, borderRadius, shadows, animations } from './layout.js';

export const theme = {
  colors,
  typography,
  spacing,
  borderRadius,
  shadows,
  animations,
};

export { colors, typography, spacing, borderRadius, shadows, animations };
